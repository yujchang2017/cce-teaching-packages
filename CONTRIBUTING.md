# 如何參與回饋與改編

歡迎老師、教育工作者、學生、家長一起試用這些教案，並回饋現場經驗或改編成果。我們目前提供兩種參與路徑：

---

## 路徑一：表單路徑（推薦給不熟悉 GitHub 的夥伴）

前往 [community.cce.tw](https://community.cce.tw)，在各教案頁面點選「試教／回饋表單」後，您可以：

1. 第一次提交時先填寫「貢獻者基本資料表」
2. 之後以已驗證 Google Email 作為投稿者識別
3. 點選「試教／回饋」開始填寫
4. 表單會預填教案編號
5. 送出後由社群審查團隊每週批次審查

**不需要懂 Git、不需要裝任何軟體**。

> 基本資料表只需第一次填寫；後續回饋會以同一個 Google Email 與基本資料對照。

---

## 路徑二：技術路徑（熟悉 GitHub 的貢獻者）

### 準備工作
- GitHub 帳號
- 基本 Git 操作（clone, branch, commit, push）
- Markdown 與 HTML 基礎編輯能力

### 步驟

1. **Fork 本倉庫** 到您的帳號下
2. **建立新分支**：命名格式 `remix/您的名字-教案編號`
   ```bash
   git checkout -b remix/wang-laoshi-1.1-III
   ```
3. **複製要改編的教案資料夾**（不要覆蓋原版）：
   ```
   packages/level-iii/1.1-III/              ← 原版，別動
   packages/level-iii/1.1-III--remix-王老師/  ← 您的改編版放這裡
   ```
4. **編輯內容**，並在改編版的 `_meta.json` 加入：
   ```json
   {
     "origin": "remix",
     "based_on": "1.1-III",
     "remixer": "王小明老師",
     "remix_notes": "改編重點：調整為都會區情境、新增氣候難民議題"
   }
   ```
5. **提交並推送**：
   ```bash
   git add -A
   git commit -m "Remix 1.1-III：都會區情境版（王小明老師）"
   git push origin remix/wang-laoshi-1.1-III
   ```
6. **送 Pull Request** 到 `main` 分支，填寫 PR 描述

### 審查流程

社群目前以 Google Form 與 Google Sheets 進行初步收件與審查：

1. 投稿者送出回饋或試教記錄後，系統通知審查團隊
2. 審查員從通知信開啟回覆試算表指定列
3. 依下方「審查員操作 SOP」檢查內容、授權與附件
4. 在試算表右側內部欄位填入審查結果
5. 若資料不足，審查員以 Email 請投稿者補件
6. 通過後，維護者整理可公開內容並合併到主庫或網站資料
7. 達成徽章或給獎條件者，另由審查團隊彙整名單

#### 審查員操作 SOP

每一筆提交都以單一「試教／回饋」類別處理，審查時依實際填寫內容檢查回饋、試教情境、教學省思、照片或學生作品授權。

基本檢查項目：

1. 身分對照：以回饋表單的 Google Email 到「貢獻者基本資料表」確認老師姓名、學校、縣市與任教年段。若查不到，狀態先填 `R`，請投稿者補填基本資料。
2. 內容完整：確認教案編號、回饋或試教內容不是空白，且能判斷投稿者實際使用了哪份教案。
3. 授權確認：照片、學生作品、改編檔案若要公開，必須有明確授權；沒有授權或授權不清楚時，只能標為僅供內部審核或請投稿者補件。
4. 個資檢查：公開素材不得含學生姓名、清楚可辨識臉部、聯絡方式、班級座號等未授權個資。若有疑慮，先填 `R`。
5. 內部欄位：審查結果只填在回覆試算表右側內部欄位，不要放回公開 Google Form。

建議的內部審查欄位：

| 欄位 | 填寫方式 |
| --- | --- |
| `狀態` | `A` 或 `R`；大小寫皆可 |
| `審核備註` | 給投稿者或維護者看的具體說明 |
| `審查委員` | 審查員姓名或 Email |
| `審查日期` | 完成審查日期 |
| `審核員評分` | 0-5 分，供給獎或徽章參考 |
| `評分維度` | 例如：在地化、附完整教案、具體學生回饋、有照片、改編試教 |
| `推薦給獎` | `是` 或空白 |
| `公開處理` | `可公開`、`去識別後公開`、`僅供內部審核` |

狀態判定：

- `A` 或 `a`：通過。內容完整、授權清楚、沒有個資疑慮，可列入首頁統計與整理公開流程。
- `R` 或 `r`：補件。缺基本資料、授權不清、附件無法開啟、內容不足或需去識別化，請投稿者補件。
- 空白：尚未審查。不列入首頁「通過回饋」與學校排行榜。

若內容與教案無關、授權不允許使用、含無法處理的個資或明顯不適合公開，可在 `狀態` 填 `R`，並於 `審核備註` 寫明「不收錄」原因。

補件信件應具體列出需要補什麼，例如「請先填基本資料表」「請補照片授權」「請移除學生姓名」「請補充試教日期與學生人數」。

審查表單與試算表權限提醒：

- 回饋／試教表單的回覆試算表必須分享給審查員，否則審查通知中的試算表連結會無法開啟
- 建議由社群管理帳號持有表單、回覆試算表與 Apps Script
- 審查員使用自己的 Google 帳號操作，方便追蹤審查紀錄
- 公開表單不應包含「審查委員」「審查狀態」「內部備註」等內部欄位；這些欄位應放在回覆試算表右側，並以保護範圍限制編輯權

技術路徑的 Pull Request 仍由維護者審查。通過後合併到 `main`，收錄為官方衍生版或公開成果。

---

## 路徑三：直接優化現有教材（PR，最推薦給小幅改進）

如果你發現某份教案的內容可以優化（修正錯誤、補充說明、改善用字、更新連結等），歡迎直接改**現有檔案**並送 Pull Request。這條路和「路徑二 改編」不同：改編是在新資料夾做衍生版本，優化則是直接讓官方版本變得更好。

### 不會 Git 也能做

1. 在 GitHub 上打開要改的檔案（例如 `packages/level-iii/3.1-III/lesson_plan.html`）
2. 按右上角鉛筆 ✏️（或在 repo 首頁按鍵盤 `.` 進入網頁版編輯器 github.dev）
3. 直接修改內容
4. 按「Commit changes」→ 選「Create a new branch and start a pull request」
5. 填一下 PR 表單（教案代號、修改類型、你的稱呼、變更摘要）後送出

送出後：

- 會有一支**自動檢查**在 PR 留言，附上你**改寫後教材的即時預覽連結**，點開就能看到成果
- 自動檢查只擋明顯的問題（夾帶密鑰、個資、壞掉的 JSON 等）；其餘一律交給維護者判斷
- **維護者會逐行看過你的修改**，覺得妥當就合併、上線；需要調整會在 PR 留言跟你討論

### 幾個小提醒

- **不用改 `version.json`**：版本紀錄由維護者在審查時統一補上
- **不要更改檔名**：`lesson_plan.html`、`ppt.html`、`worksheet.html` 請保持原名，網站連結才不會斷
- 大幅重寫前，可以先開 Issue 或在 PR 說明你的想法，避免白做工

> 想做的是「另立一個改編版本」而不是優化官方版？請走上面的**路徑二**。

---

## 授權須知

- 您的改編版會**自動沿用 CC BY-SA 4.0**
- 請在改編版的 `README.md` 標註原作者與您自己
- 商業使用需另行洽談（聯絡窗口見主 README）

---

## 回饋與提問

- 教學試教回饋：請從網站各教案頁面的「試教／回饋表單」提交
- 改編前構想討論：可使用 [Remix Proposal Issue Template](.github/ISSUE_TEMPLATE/remix-proposal.md)
- 技術問題或網站錯誤：可使用 GitHub Issue 回報

感謝您的參與，讓氣候變遷教育更貼近臺灣的孩子。
